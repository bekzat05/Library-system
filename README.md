# Library-system
Library system
CRUD project using Spring Core, Spring MVC, JDBC template, PostgreSQL
Must be run with Apache Tomcat

Each Person has a page with a list of books assigned to him. 
Person can be created, edited, deleted, updated.

Each Book has a page with a Person to which it was assigned, or if the book is free, has a list of People to which it can be assigned
